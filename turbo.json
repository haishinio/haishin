{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "faker": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "./dist/**"]
    },
    "start": {
      "dependsOn": ["build", "lint"]
    }
  },
  "globalEnv": [
    "APP_ENV",
    "FFMPEG_PATH",
    "HAISHIN_VERSION",
    "OPENAI_API_KEY",
    "DEEPL_API_KEY",
    "NODE_ENV",
    "PRODUCTION_URL",
    "FRONTEND_URL",
    "RTMP_SERVER_URL",
    "RTMP_CLIENT_API_URL",
    "NEXT_PUBLIC_RTMP_CLIENT_API_URL",
    "RTMP_CLIENT_URL",
    "WS_URL",
    "SENTRY_DSN",
    "NEXT_PUBLIC_SENTRY_DSN",
    "SENTRY_IGNORE_API_RESOLUTION_ERROR"
  ]
}
